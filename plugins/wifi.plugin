#!/bin/sh

if [ -z "$TIMESTAMP" ] ; then
	TIMESTAMP=44444444
fi
if [ -z "$PREFIX" ] ; then
	PREFIX=FOO
fi

for phy in $(ls /sys/class/ieee80211/) ; do
	for dev in $(ls /sys/class/ieee80211/$phy/device/net/) ; do
		iw $dev station dump | awk -v timestamp="$TIMESTAMP" -v prefix="${PREFIX}.wifi" -v FS='\t' -f "$(dirname $0)/wifi.awk"
	done
done
